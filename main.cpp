import Compiler;

import <string>;
import <iostream>;
import <vector>;

int main(int argc, char* argv[]) {
	std::vector<std::string> commands{};
	commands.assign(argv + 1, argv + argc);

  return 0;
}